# Examples

This folder will contain representative output from real uses of this skill — sanitized to remove organization-specific details — so you can see what finished output actually looks like before running the skill yourself.

Examples are added when an output is general enough to publish without sanitization concerns.
